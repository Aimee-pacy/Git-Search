import { CaloringDirective } from './caloring.directive';

describe('CaloringDirective', () => {
  it('should create an instance', () => {
    const directive = new CaloringDirective();
    expect(directive).toBeTruthy();
  });
});
